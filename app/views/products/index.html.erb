<div class="center jumbotron">
	<h1>All Products</h1>
	<!-- Commented from here to
	<h4>Already a customer
	<%= link_to 'Login',login_path, class: "btn btn-lg btn-primary"%>
	</h4>

	<h4>Else
	<%= link_to 'SignUp',new_customer_path, class: "btn btn-lg btn-primary"%>
	</h4>
	here -->
	<h4>
	<%= link_to 'Add new product',new_product_path, class: "btn btn-lg btn-primary"%>
	</h4>
	<br>
	
</div>

<table cellspacing="20">
	<tr>
		<th>Name</th>
		<th>Brand</th>
		<th>Price</th>
		<th>Size</th>
		<th>Category</th>
		<th>Updated At</th>
	</tr>
	
	<% @products.each do |product| %>
		<tr>
			<td><%= product.name %></td>
			<td><%= product.brand %></td>
			<td><%= product.price %></td>
			<td><%= product.size %></td>
			<td><%= product.category %></td>
			<td><%= product.updated_at %></td>
			<td><%= link_to 'Show',product_path(product)%></td>
			<td><%= link_to 'Buy','#'%></td>
			<td><%= link_to 'Destroy',product_path(product), method: :delete, data: {confirm: 'Are you sure?'}%></td>
		</tr>
	<% end %>

</table>